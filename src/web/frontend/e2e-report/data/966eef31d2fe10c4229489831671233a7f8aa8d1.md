# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> 认证流程 E2E 测试 >> 登出后跳转到登录页
- Location: e2e\auth.spec.ts:46:3

# Error details

```
TimeoutError: page.waitForURL: Timeout 15000ms exceeded.
=========================== logs ===========================
waiting for navigation to "**/dashboard**" until "load"
============================================================
```

# Page snapshot

```yaml
- generic [ref=e4]:
  - heading "用户登录" [level=2] [ref=e6]
  - generic [ref=e8]:
    - generic [ref=e9]:
      - generic [ref=e10]: "* 用户名"
      - textbox "* 用户名" [ref=e14]:
        - /placeholder: 请输入用户名
        - text: v21test_admin
    - generic [ref=e15]:
      - generic [ref=e16]: "* 密码"
      - generic [ref=e19]:
        - textbox "* 密码" [ref=e20]:
          - /placeholder: 请输入密码
          - text: Test1234!
        - img [ref=e23] [cursor=pointer]
    - generic [ref=e28] [cursor=pointer]:
      - generic [ref=e29]:
        - checkbox "记住我"
      - generic [ref=e31]: 记住我
    - button "登录" [ref=e34] [cursor=pointer]:
      - generic [ref=e35]: 登录
    - generic [ref=e36]:
      - link "忘记密码？" [ref=e37] [cursor=pointer]:
        - /url: /forgot-password
      - text: "|"
      - link "还没有账号？立即注册" [ref=e38] [cursor=pointer]:
        - /url: /register
```

# Test source

```ts
  1  | import { test, expect, ADMIN_USER, WRONG_USER } from './helpers'
  2  | 
  3  | test.describe('认证流程 E2E 测试', () => {
  4  | 
  5  |   test('登录页面正确渲染', async ({ page }) => {
  6  |     await page.goto('/login')
  7  |     await expect(page.locator('input[type="text"], input[placeholder*="用户名"]')).toBeVisible()
  8  |     await expect(page.locator('input[type="password"]')).toBeVisible()
  9  |     await expect(page.locator('button[type="submit"], button:has-text("登录")')).toBeVisible()
  10 |   })
  11 | 
  12 |   test('正确凭证登录成功并跳转到首页', async ({ page }) => {
  13 |     await page.goto('/login')
  14 |     await page.fill('input[placeholder*="用户名"], input[type="text"]', ADMIN_USER.username)
  15 |     await page.fill('input[placeholder*="密码"], input[type="password"]', ADMIN_USER.password)
  16 |     await page.click('button[type="submit"], button:has-text("登录")')
  17 |     await page.waitForURL('**/dashboard**', { timeout: 15000 })
  18 |     await expect(page).toHaveURL(/\/dashboard/)
  19 |   })
  20 | 
  21 |   test('错误凭证登录失败显示错误提示', async ({ page }) => {
  22 |     await page.goto('/login')
  23 |     await page.fill('input[placeholder*="用户名"], input[type="text"]', WRONG_USER.username)
  24 |     await page.fill('input[placeholder*="密码"], input[type="password"]', WRONG_USER.password)
  25 |     await page.click('button[type="submit"], button:has-text("登录")')
  26 |     await page.waitForTimeout(2000)
  27 |     const hasError = await page.locator('.el-message--error, .el-form-item__error, [class*="error"]').count() > 0
  28 |     const stillOnLogin = page.url().includes('/login')
  29 |     expect(hasError || stillOnLogin).toBeTruthy()
  30 |   })
  31 | 
  32 |   test('未登录访问受保护页面重定向到登录页', async ({ page }) => {
  33 |     await page.goto('/diagnosis')
  34 |     await page.waitForTimeout(2000)
  35 |     const url = page.url()
  36 |     expect(url).toContain('/login')
  37 |   })
  38 | 
  39 |   test('未登录访问管理后台重定向到登录页', async ({ page }) => {
  40 |     await page.goto('/admin')
  41 |     await page.waitForTimeout(2000)
  42 |     const url = page.url()
  43 |     expect(url).toContain('/login')
  44 |   })
  45 | 
  46 |   test('登出后跳转到登录页', async ({ page }) => {
  47 |     await page.goto('/login')
  48 |     await page.fill('input[placeholder*="用户名"], input[type="text"]', ADMIN_USER.username)
  49 |     await page.fill('input[placeholder*="密码"], input[type="password"]', ADMIN_USER.password)
  50 |     await page.click('button[type="submit"], button:has-text("登录")')
> 51 |     await page.waitForURL('**/dashboard**', { timeout: 15000 })
     |                ^ TimeoutError: page.waitForURL: Timeout 15000ms exceeded.
  52 | 
  53 |     const logoutBtn = page.locator('button:has-text("退出"), button:has-text("登出"), [class*="logout"], [class*="logout"]')
  54 |     if (await logoutBtn.count() > 0) {
  55 |       await logoutBtn.first().click()
  56 |       await page.waitForTimeout(2000)
  57 |       expect(page.url()).toContain('/login')
  58 |     } else {
  59 |       const dropdown = page.locator('.el-dropdown, [class*="avatar"], [class*="user"]')
  60 |       if (await dropdown.count() > 0) {
  61 |         await dropdown.first().click()
  62 |         await page.waitForTimeout(500)
  63 |         const logoutLink = page.locator('text=退出, text=登出, text=注销')
  64 |         if (await logoutLink.count() > 0) {
  65 |           await logoutLink.first().click()
  66 |           await page.waitForTimeout(2000)
  67 |           expect(page.url()).toContain('/login')
  68 |         }
  69 |       }
  70 |     }
  71 |   })
  72 | })
  73 | 
```